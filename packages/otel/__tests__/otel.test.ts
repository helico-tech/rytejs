import { describe, expect, test } from "vitest";

describe("@rytejs/otel", () => {
	test("package exports", () => {
		// Placeholder — real tests are added with each module
		expect(true).toBe(true);
	});
});
