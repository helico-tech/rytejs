# E2E Tests Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** A standalone test project that installs `@rytejs/core` from npm and runs assertions against it, verifying the published package works for real consumers.

**Architecture:** `examples/e2e` is excluded from the pnpm workspace so it installs from the npm registry. A CI workflow reads the version from `packages/core/package.json`, updates the E2E dependency, installs, and runs vitest.

**Tech Stack:** TypeScript, Vitest, npm (not pnpm — standalone project), GitHub Actions

**Spec:** `docs/superpowers/specs/2026-03-14-e2e-tests-design.md`

---

## File Structure

- **Modify:** `pnpm-workspace.yaml` — exclude `examples/e2e`
- **Create:** `examples/e2e/package.json` — standalone project depending on `@rytejs/core` from npm
- **Create:** `examples/e2e/tsconfig.json` — TypeScript config for the E2E project
- **Create:** `examples/e2e/e2e.test.ts` — test file showcasing real usage with assertions
- **Create:** `.github/workflows/e2e.yml` — CI workflow

---

## Task 1: Scaffold the E2E project

**Files:**
- Modify: `pnpm-workspace.yaml`
- Create: `examples/e2e/package.json`
- Create: `examples/e2e/tsconfig.json`

- [ ] **Step 1: Exclude `examples/e2e` from pnpm workspace**

Update `pnpm-workspace.yaml` to:

```yaml
packages:
  - "packages/*"
  - "docs"
  - "examples/*"
  - "!examples/e2e"
```

- [ ] **Step 2: Create `examples/e2e/package.json`**

```json
{
  "name": "@rytejs/example-e2e",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run"
  },
  "dependencies": {
    "@rytejs/core": "0.2.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "vitest": "^3.0.0",
    "typescript": "^5.7.0"
  }
}
```

- [ ] **Step 3: Create `examples/e2e/tsconfig.json`**

```json
{
  "compilerOptions": {
    "strict": true,
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "esModuleInterop": true,
    "skipLibCheck": true,
    "noEmit": true
  },
  "include": ["*.ts"]
}
```

- [ ] **Step 4: Reinstall workspace deps**

Run: `pnpm install`
Expected: `examples/e2e` is NOT listed as a workspace package

- [ ] **Step 5: Commit**

```bash
git add pnpm-workspace.yaml examples/e2e/package.json examples/e2e/tsconfig.json
git commit -m "chore: scaffold e2e test project (excluded from workspace)"
```

---

## Task 2: Write the E2E test file

**Files:**
- Create: `examples/e2e/e2e.test.ts`

- [ ] **Step 1: Write the test file**

This is a living example that showcases the full API surface with assertions:

```ts
import { describe, expect, test } from "vitest";
import { z } from "zod";
import { defineWorkflow, WorkflowRouter, createKey } from "@rytejs/core";

// ─── Define a workflow ───────────────────────────────────────────────

const taskWorkflow = defineWorkflow("task", {
  states: {
    Todo: z.object({ title: z.string(), assignee: z.string().optional() }),
    InProgress: z.object({
      title: z.string(),
      assignee: z.string(),
      startedAt: z.coerce.date(),
    }),
    Done: z.object({
      title: z.string(),
      assignee: z.string(),
      completedAt: z.coerce.date(),
    }),
  },
  commands: {
    Assign: z.object({ assignee: z.string() }),
    Start: z.object({}),
    Complete: z.object({}),
  },
  events: {
    TaskAssigned: z.object({ taskId: z.string(), assignee: z.string() }),
    TaskStarted: z.object({ taskId: z.string() }),
    TaskCompleted: z.object({ taskId: z.string() }),
  },
  errors: {
    NotAssigned: z.object({}),
  },
});

// ─── Fluent router setup ─────────────────────────────────────────────

const todoRouter = new WorkflowRouter(taskWorkflow)
  .state("Todo", (state) => {
    state
      .on("Assign", (ctx) => {
        ctx.update({ assignee: ctx.command.payload.assignee });
        ctx.emit({
          type: "TaskAssigned",
          data: {
            taskId: ctx.workflow.id,
            assignee: ctx.command.payload.assignee,
          },
        });
      })
      .on("Start", (ctx) => {
        const { assignee } = ctx.data;
        if (!assignee) {
          ctx.error({ code: "NotAssigned", data: {} });
          return;
        }
        ctx.transition("InProgress", {
          title: ctx.data.title,
          assignee,
          startedAt: new Date(),
        });
        ctx.emit({ type: "TaskStarted", data: { taskId: ctx.workflow.id } });
      });
  });

const inProgressRouter = new WorkflowRouter(taskWorkflow)
  .state("InProgress", (state) => {
    state.on("Complete", (ctx) => {
      ctx.transition("Done", {
        title: ctx.data.title,
        assignee: ctx.data.assignee,
        completedAt: new Date(),
      });
      ctx.emit({
        type: "TaskCompleted",
        data: { taskId: ctx.workflow.id },
      });
    });
  });

// ─── Compose routers ────────────────────────────────────────────────

const router = new WorkflowRouter(taskWorkflow)
  .use(todoRouter)
  .use(inProgressRouter);

// ─── Tests ──────────────────────────────────────────────────────────

describe("@rytejs/core E2E", () => {
  test("state transition: Todo → Todo (update)", async () => {
    const task = taskWorkflow.createWorkflow("task-1", {
      initialState: "Todo",
      data: { title: "Write docs" },
    });

    const result = await router.dispatch(task, {
      type: "Assign",
      payload: { assignee: "alice" },
    });

    expect(result.ok).toBe(true);
    if (!result.ok) throw new Error();
    expect(result.workflow.state).toBe("Todo");
    if (result.workflow.state === "Todo") {
      expect(result.workflow.data.assignee).toBe("alice");
    }
  });

  test("event emission", async () => {
    const task = taskWorkflow.createWorkflow("task-2", {
      initialState: "Todo",
      data: { title: "Ship it" },
    });

    const result = await router.dispatch(task, {
      type: "Assign",
      payload: { assignee: "bob" },
    });

    expect(result.ok).toBe(true);
    if (!result.ok) throw new Error();
    expect(result.events).toHaveLength(1);
    expect(result.events[0]?.type).toBe("TaskAssigned");
  });

  test("domain error with rollback", async () => {
    const task = taskWorkflow.createWorkflow("task-3", {
      initialState: "Todo",
      data: { title: "No assignee" },
    });

    const result = await router.dispatch(task, {
      type: "Start",
      payload: {},
    });

    expect(result.ok).toBe(false);
    if (result.ok) throw new Error();
    expect(result.error.category).toBe("domain");
    if (result.error.category === "domain") {
      expect(result.error.code).toBe("NotAssigned");
    }
    // Original workflow unchanged (rollback)
    expect(task.state).toBe("Todo");
  });

  test("full lifecycle: Todo → InProgress → Done", async () => {
    let task = taskWorkflow.createWorkflow("task-4", {
      initialState: "Todo",
      data: { title: "Full lifecycle", assignee: "alice" },
    });

    let result = await router.dispatch(task, {
      type: "Start",
      payload: {},
    });
    expect(result.ok).toBe(true);
    if (!result.ok) throw new Error();
    expect(result.workflow.state).toBe("InProgress");

    result = await router.dispatch(result.workflow, {
      type: "Complete",
      payload: {},
    });
    expect(result.ok).toBe(true);
    if (!result.ok) throw new Error();
    expect(result.workflow.state).toBe("Done");
  });

  test("composable routers: handlers from child routers work", async () => {
    // todoRouter and inProgressRouter were composed into router above
    // Verify both contribute handlers
    const task = taskWorkflow.createWorkflow("task-5", {
      initialState: "Todo",
      data: { title: "Composed" },
    });

    // Handler from todoRouter
    const r1 = await router.dispatch(task, {
      type: "Assign",
      payload: { assignee: "charlie" },
    });
    expect(r1.ok).toBe(true);

    // Handler from inProgressRouter
    const started = taskWorkflow.createWorkflow("task-6", {
      initialState: "InProgress",
      data: { title: "Started", assignee: "charlie", startedAt: new Date() },
    });
    const r2 = await router.dispatch(started, {
      type: "Complete",
      payload: {},
    });
    expect(r2.ok).toBe(true);
    if (r2.ok) expect(r2.workflow.state).toBe("Done");
  });

  test("context keys with middleware", async () => {
    const UserKey = createKey<string>("user");

    const authedRouter = new WorkflowRouter(taskWorkflow)
      .use(async (ctx, next) => {
        ctx.set(UserKey, "admin");
        await next();
      })
      .use(todoRouter);

    const task = taskWorkflow.createWorkflow("task-7", {
      initialState: "Todo",
      data: { title: "With middleware" },
    });

    const result = await authedRouter.dispatch(task, {
      type: "Assign",
      payload: { assignee: "dave" },
    });
    expect(result.ok).toBe(true);
  });
});
```

- [ ] **Step 2: Install E2E deps and run tests**

```bash
cd examples/e2e
npm install
npx vitest run
```

Expected: All 6 tests PASS

- [ ] **Step 3: Commit**

```bash
git add examples/e2e/e2e.test.ts
git commit -m "test: add E2E tests against published @rytejs/core"
```

---

## Task 3: CI workflow

**Files:**
- Create: `.github/workflows/e2e.yml`

- [ ] **Step 1: Create the workflow**

```yaml
name: E2E

on:
  push:
    branches: [master]

jobs:
  e2e:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Read package version
        id: version
        run: echo "version=$(node -p "require('./packages/core/package.json').version")" >> "$GITHUB_OUTPUT"

      - name: Set @rytejs/core version in E2E project
        run: |
          cd examples/e2e
          node -e "
            const pkg = require('./package.json');
            pkg.dependencies['@rytejs/core'] = '${{ steps.version.outputs.version }}';
            require('fs').writeFileSync('package.json', JSON.stringify(pkg, null, 2) + '\n');
          "

      - name: Install E2E dependencies
        run: |
          cd examples/e2e
          npm install

      - name: Run E2E tests
        run: |
          cd examples/e2e
          npx vitest run
```

- [ ] **Step 2: Commit**

```bash
git add .github/workflows/e2e.yml
git commit -m "ci: add E2E workflow testing published npm package"
```

---

## Task 4: Final verification and push

- [ ] **Step 1: Run E2E tests locally**

```bash
cd examples/e2e && npm install && npx vitest run
```

Expected: All tests PASS

- [ ] **Step 2: Run workspace check to verify no regressions**

```bash
pnpm check
```

Expected: All 96 unit tests pass, typecheck passes, biome clean

- [ ] **Step 3: Push**

```bash
git push
```
